name: CI
on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Intro
        run: |
          echo "Intro to Release"
          which python
          python --version
          which pip
          pip --version
